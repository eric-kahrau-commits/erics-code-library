import type { LibraryConfig, LibraryEntry } from '../types/index.js'

const entries: LibraryEntry[] = [
  {
    id: 'ts-types',
    name: 'Grundlegende Typen',
    category: 'Typen',
    description: 'TypeScript fügt statische Typen zu JavaScript hinzu. Typfehler werden bereits beim Schreiben des Codes erkannt, nicht erst zur Laufzeit.',
    syntax: '// Primitive Typen\nlet name: string = "Anna";\nlet alter: number = 25;\nlet aktiv: boolean = true;\nlet leer: null = null;\nlet undefiniert: undefined = undefined;\n\n// any - deaktiviert die Typüberprüfung (vermeiden!)\nlet x: any = 42;\nx = "String"; // Erlaubt, aber unsicher\n\n// unknown - sicherer als any\nlet eingabe: unknown = holeEingabe();\nif (typeof eingabe === "string") {\n  console.log(eingabe.toUpperCase()); // Sicher!\n}',
    language: 'typescript',
    tags: ['typ', 'type', 'string', 'number', 'boolean', 'any', 'unknown', 'grundlagen'],
  },
  {
    id: 'ts-interfaces',
    name: 'Interfaces',
    category: 'Interfaces & Types',
    description: 'Interfaces definieren die Form eines Objekts. Eigenschaften können optional (`?`) oder schreibgeschützt (`readonly`) sein.',
    syntax: 'interface Benutzer {\n  id: number;\n  name: string;\n  email: string;\n  alter?: number;         // Optional\n  readonly erstellt: Date; // Nicht veränderbar\n}\n\n// Verwendung\nconst user: Benutzer = {\n  id: 1,\n  name: "Max",\n  email: "max@web.de",\n  erstellt: new Date(),\n};\n\n// Interface erweitern\ninterface Admin extends Benutzer {\n  rechte: string[];\n  bereich: "global" | "lokal";\n}',
    language: 'typescript',
    tags: ['interface', 'objekt', 'typ', 'optional', 'readonly', 'struktur', 'form'],
  },
  {
    id: 'ts-type-alias',
    name: 'Type Aliases',
    category: 'Interfaces & Types',
    description: 'Type Aliases (`type`) sind ähnlich wie Interfaces, können aber auch für primitive Typen, Unions und komplexe Typen verwendet werden.',
    syntax: '// Einfacher Alias\ntype ID = string | number;\ntype Farbe = "rot" | "grün" | "blau"; // Union Literal\n\n// Objekt-Typ\ntype Punkt = {\n  x: number;\n  y: number;\n};\n\n// Funktion\ntype Callback = (fehler: Error | null, data: string) => void;\n\n// Intersect: beide Typen kombinieren\ntype Benutzer = { name: string };\ntype Admin = Benutzer & { rechte: string[] };\n\nconst admin: Admin = { name: "Lisa", rechte: ["lesen", "schreiben"] };',
    language: 'typescript',
    tags: ['type', 'alias', 'union', 'literal', 'intersect', 'typ-alias', 'alias'],
  },
  {
    id: 'ts-generics',
    name: 'Generics',
    category: 'Generics',
    description: 'Generics ermöglichen typsichere Funktionen und Klassen, die mit verschiedenen Datentypen funktionieren. `<T>` ist ein Platzhalter für den Typ.',
    syntax: '// Generische Funktion\nfunction erstes<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\n\nconst zahl = erstes([1, 2, 3]);     // TypeScript weiß: number\nconst wort  = erstes(["a", "b"]);   // TypeScript weiß: string\n\n// Generisches Interface\ninterface ApiAntwort<T> {\n  data: T;\n  status: number;\n  nachricht: string;\n}\n\nconst antwort: ApiAntwort<Benutzer[]> = {\n  data: [...],\n  status: 200,\n  nachricht: "OK",\n};\n\n// Einschränkung mit extends\nfunction holeName<T extends { name: string }>(obj: T): string {\n  return obj.name;\n}',
    language: 'typescript',
    tags: ['generics', 'generic', 'platzhalter', 'typ', 't', 'flexibel', 'typsicher'],
  },
  {
    id: 'ts-enums',
    name: 'Enums',
    category: 'Typen',
    description: 'Enums definieren eine Menge benannter Konstanten. String-Enums sind zu bevorzugen, da sie im Code besser lesbar sind.',
    syntax: '// Numerisches Enum (Standard)\nenum Richtung {\n  Oben,    // 0\n  Rechts,  // 1\n  Unten,   // 2\n  Links,   // 3\n}\n\n// String-Enum (bevorzugt!)\nenum Status {\n  Aktiv = "AKTIV",\n  Inaktiv = "INAKTIV",\n  Gesperrt = "GESPERRT",\n}\n\n// Verwendung\nfunction zeigeStatus(s: Status): void {\n  if (s === Status.Aktiv) {\n    console.log("Benutzer ist aktiv!");\n  }\n}\nzeigeStatus(Status.Aktiv);',
    language: 'typescript',
    tags: ['enum', 'konstante', 'aufzählung', 'string-enum', 'status', 'benannt'],
  },
  {
    id: 'ts-union-intersection',
    name: 'Union & Intersection Types',
    category: 'Typen',
    description: 'Union (`|`) bedeutet "entweder oder". Intersection (`&`) bedeutet "und" – beide Typen werden kombiniert.',
    syntax: '// Union: kann einer von mehreren Typen sein\ntype ID = string | number;\ntype Ergebnis = string | null;\ntype Eingabe = string | number | boolean;\n\nfunction verarbeite(wert: string | number): string {\n  if (typeof wert === "string") return wert.toUpperCase();\n  return String(wert);\n}\n\n// Intersection: hat alle Eigenschaften beider Typen\ntype Serialisierbar = { toJSON(): string };\ntype Vergleichbar = { compareTo(other: unknown): number };\n\ntype SerializierbaresObjekt = Serialisierbar & Vergleichbar;\n\n// Discriminated Union (Pattern)\ntype Form =\n  | { art: "kreis"; radius: number }\n  | { art: "rechteck"; breite: number; hoehe: number };',
    language: 'typescript',
    tags: ['union', 'intersection', 'oder', 'und', 'typ', 'kombinieren', 'discriminated'],
  },
  {
    id: 'ts-type-guards',
    name: 'Type Guards',
    category: 'Typen',
    description: 'Type Guards ermöglichen es, TypeScript zur Laufzeit über den konkreten Typ zu informieren. Damit werden Union Types sicher.',
    syntax: '// typeof Guard\nfunction berechne(wert: string | number): number {\n  if (typeof wert === "string") {\n    return parseInt(wert); // wert: string\n  }\n  return wert; // wert: number\n}\n\n// instanceof Guard\nclass Hund { bellen() { return "Wuff!"; } }\nclass Katze { schnurren() { return "Purr..."; } }\n\nfunction reagiere(tier: Hund | Katze): void {\n  if (tier instanceof Hund) {\n    console.log(tier.bellen());\n  } else {\n    console.log(tier.schnurren());\n  }\n}\n\n// Custom Type Guard (is-Syntax)\nfunction istHund(tier: Hund | Katze): tier is Hund {\n  return (tier as Hund).bellen !== undefined;\n}',
    language: 'typescript',
    tags: ['type guard', 'typeof', 'instanceof', 'sicher', 'prüfen', 'narrowing', 'union'],
  },
  {
    id: 'ts-utility-types',
    name: 'Utility Types',
    category: 'Utility Types',
    description: 'TypeScript bietet eingebaute Utility Types, die Typen transformieren. Die wichtigsten: `Partial`, `Required`, `Readonly`, `Pick`, `Omit`.',
    syntax: 'interface Benutzer {\n  id: number;\n  name: string;\n  email: string;\n  passwort: string;\n}\n\n// Partial - alle Properties optional\ntype BenutzerUpdate = Partial<Benutzer>;\n\n// Required - alle Properties erforderlich\ntype BenutzerKomplett = Required<Benutzer>;\n\n// Readonly - unveränderlich\ntype FrozenUser = Readonly<Benutzer>;\n\n// Pick - nur bestimmte Properties\ntype PublicUser = Pick<Benutzer, "id" | "name">;\n\n// Omit - Properties ausschließen\ntype OhnePasswort = Omit<Benutzer, "passwort">;\n\n// Record\ntype RollenMap = Record<string, string[]>;\n// { admin: ["lesen", "schreiben"], user: ["lesen"] }',
    language: 'typescript',
    tags: ['utility', 'partial', 'required', 'readonly', 'pick', 'omit', 'record', 'eingebaut'],
  },
  {
    id: 'ts-optional-chaining',
    name: 'Optional Chaining & Nullish',
    category: 'Moderne Syntax',
    description: '`?.` (Optional Chaining) greift sicher auf Eigenschaften zu, ohne Fehler bei `null`/`undefined`. `??` (Nullish Coalescing) gibt einen Fallback-Wert zurück.',
    syntax: 'interface Nutzer {\n  name: string;\n  adresse?: {\n    stadt?: string;\n    plz?: string;\n  };\n}\n\nconst nutzer: Nutzer | null = holeNutzer();\n\n// Ohne Optional Chaining (fehleranfällig)\nconst stadt = nutzer && nutzer.adresse && nutzer.adresse.stadt;\n\n// Mit Optional Chaining (?.) – sauber!\nconst stadt = nutzer?.adresse?.stadt;\n// Rückgabe: string | undefined (kein Fehler!)\n\n// Nullish Coalescing (??)\nconst anzeigeName = nutzer?.name ?? "Gast";\n// Nur wenn name null oder undefined, wird "Gast" verwendet\n\n// Nullish Assignment (??=)\nnutzer.name ??= "Standardname";',
    language: 'typescript',
    tags: ['optional chaining', 'nullish', 'fragezeichen', 'null', 'undefined', 'sicher', 'fallback'],
  },
  {
    id: 'ts-decorators',
    name: 'Funktionen & Überladen',
    category: 'Funktionen',
    description: 'TypeScript ermöglicht das Überladen von Funktionen: Mehrere Signaturen, eine Implementierung. Klare Typisierung von Parametern und Rückgabetypen.',
    syntax: '// Funktionen mit Typen\nfunction begruesse(name: string, foermlich?: boolean): string {\n  if (foermlich) {\n    return `Guten Tag, ${name}.`;\n  }\n  return `Hey, ${name}!`;\n}\n\n// Function Overloading\nfunction verarbeite(wert: string): string;\nfunction verarbeite(wert: number): number;\nfunction verarbeite(wert: string | number): string | number {\n  if (typeof wert === "string") return wert.toUpperCase();\n  return wert * 2;\n}\n\n// Arrow Function mit Typen\nconst addiere = (a: number, b: number): number => a + b;\n\n// Void-Rückgabe\nconst logge = (msg: string): void => {\n  console.log(msg);\n};',
    language: 'typescript',
    tags: ['funktion', 'überladung', 'overload', 'rückgabetyp', 'parameter', 'void', 'typescript'],
  },
  {
    id: 'ts-as-const',
    name: 'as const & satisfies',
    category: 'Moderne Syntax',
    description: '`as const` macht einen Wert unveränderlich und narrowt den Typ so eng wie möglich. `satisfies` prüft den Typ ohne ihn zu widening.',
    syntax: '// as const - einfrieren und engen Typ\nconst FARBEN = ["rot", "grün", "blau"] as const;\n// Typ: readonly ["rot", "grün", "blau"] statt string[]\n\nconst EINSTELLUNGEN = {\n  theme: "light",\n  sprache: "de",\n  version: 3,\n} as const;\n// Alle Properties sind readonly und literal-typisiert\n\ntype Farbe = typeof FARBEN[number]; // "rot" | "grün" | "blau"\n\n// satisfies - Typprüfung ohne Widening\nconst palette = {\n  rot: [255, 0, 0],\n  grün: "#00ff00",\n} satisfies Record<string, string | number[]>;\n// palette.rot ist number[] (nicht string | number[])!',
    language: 'typescript',
    tags: ['as const', 'satisfies', 'readonly', 'literal', 'typeof', 'einfrieren', 'typ'],
  },
  // ── Fortgeschrittene Typen ────────────────────────────────────────────────────
  {
    id: 'ts-conditional-types',
    name: 'Conditional Types',
    category: 'Typen',
    description: 'Conditional Types sind Typ-Level-If/Else: `T extends U ? X : Y`. Sie erlauben Typen zu berechnen und sind die Basis vieler eingebauter Utility Types.',
    syntax: '// Grundform: T extends U ? X : Y\ntype IstString<T> = T extends string ? true : false;\n\ntype A = IstString<string>;  // true\ntype B = IstString<number>;  // false\n\n// Unwrap: Array-Typ extrahieren\ntype UnwrapArray<T> = T extends Array<infer U> ? U : T;\n\ntype C = UnwrapArray<string[]>;  // string\ntype D = UnwrapArray<number[]>;  // number\ntype E = UnwrapArray<boolean>;   // boolean (kein Array)\n\n// NonNullable (eingebaut, aber so implementiert):\ntype MeinNonNullable<T> = T extends null | undefined ? never : T;\ntype F = MeinNonNullable<string | null>; // string\n\n// Distributive Conditional Types\ntype ToArray<T> = T extends unknown ? T[] : never;\ntype G = ToArray<string | number>; // string[] | number[]',
    language: 'typescript',
    tags: ['conditional type', 'extends', 'ternary', 'typ-level', 'infer', 'bedingt', 'fortgeschritten'],
  },
  {
    id: 'ts-mapped-types',
    name: 'Mapped Types',
    category: 'Typen',
    description: 'Mapped Types iterieren über alle Keys eines Typs und transformieren ihn. Sie sind die Basis von `Partial`, `Readonly`, `Record` u.a.',
    syntax: 'interface Benutzer {\n  name: string;\n  alter: number;\n  aktiv: boolean;\n}\n\n// Alle Properties optional machen (wie Partial<T>)\ntype AllesOptional<T> = {\n  [K in keyof T]?: T[K];\n};\n\n// Alle Properties readonly machen (wie Readonly<T>)\ntype AllesReadonly<T> = {\n  readonly [K in keyof T]: T[K];\n};\n\n// Typen transformieren\ntype ZuString<T> = {\n  [K in keyof T]: string;\n};\n\n// Mit Modifier entfernen (-? = required machen, -readonly = änderbar)\ntype Required<T> = {\n  [K in keyof T]-?: T[K]; // -? entfernt optional\n};\n\n// Schlüssel filtern mit as\ntype NurStrings<T> = {\n  [K in keyof T as T[K] extends string ? K : never]: T[K];\n};',
    language: 'typescript',
    tags: ['mapped type', 'keyof', 'in', 'transform', 'partial', 'readonly', 'typ-iteration'],
  },
  {
    id: 'ts-template-literal-types',
    name: 'Template Literal Types',
    category: 'Typen',
    description: 'Template Literal Types kombinieren String-Literale auf Typ-Ebene. Erlauben präzise String-Typen für Event-Namen, CSS-Properties, API-Routen etc.',
    syntax: '// Einfache Kombination\ntype Richtung = "links" | "rechts" | "oben" | "unten";\ntype Aktion = `gehe-${Richtung}`;\n// "gehe-links" | "gehe-rechts" | "gehe-oben" | "gehe-unten"\n\n// CSS-Property-Typen\ntype Seite = "top" | "right" | "bottom" | "left";\ntype CssPadding = `padding-${Seite}`;\n// "padding-top" | "padding-right" | ...\n\n// Event-Listener Typen\ntype Events = "click" | "focus" | "blur";\ntype Handler = `on${Capitalize<Events>}`;\n// "onClick" | "onFocus" | "onBlur"\n\n// String-Manipulation (eingebaut)\ntype Gross = Uppercase<"hallo">;   // "HALLO"\ntype Klein = Lowercase<"HALLO">;   // "hallo"\ntype Erster = Capitalize<"welt">;  // "Welt"\n\n// Getter-Typen aus Objekt ableiten\ninterface Person { name: string; alter: number; }\ntype GetterKeys = `get${Capitalize<keyof Person & string>}`;\n// "getName" | "getAlter"',
    language: 'typescript',
    tags: ['template literal type', 'string-typ', 'capitalize', 'uppercase', 'kombinieren', 'event', 'css'],
  },
  {
    id: 'ts-infer',
    name: 'infer – Typen extrahieren',
    category: 'Typen',
    description: '`infer` extrahiert Typen innerhalb von Conditional Types. Damit können Rückgabetypen, Parameter-Typen oder enthaltene Typen aus komplexen Typen herausgezogen werden.',
    syntax: '// Rückgabetyp einer Funktion extrahieren\ntype ReturnType<T> = T extends (...args: any[]) => infer R ? R : never;\n\nfunction addiere(a: number, b: number): number { return a + b; }\ntype Ergebnis = ReturnType<typeof addiere>; // number\n\n// Parameter-Typ extrahieren\ntype ErsterParameter<T> =\n  T extends (arg: infer P, ...rest: any[]) => any ? P : never;\n\ntype P = ErsterParameter<(name: string, alter: number) => void>; // string\n\n// Promise-Wert extrahieren\ntype Awaited<T> = T extends Promise<infer U> ? Awaited<U> : T;\n\ntype A = Awaited<Promise<string>>;           // string\ntype B = Awaited<Promise<Promise<number>>>;  // number\n\n// Array-Element-Typ\ntype ElementTyp<T> = T extends ReadonlyArray<infer Item> ? Item : never;\ntype C = ElementTyp<string[]>;    // string\ntype D = ElementTyp<[1, 2, 3]>;   // 1 | 2 | 3',
    language: 'typescript',
    tags: ['infer', 'extrahieren', 'returntype', 'parameter', 'conditional', 'fortgeschritten', 'typ'],
  },
  {
    id: 'ts-abstract-class',
    name: 'Abstrakte Klassen',
    category: 'Klassen & OOP',
    description: 'Abstrakte Klassen können nicht direkt instanziiert werden. Sie definieren eine Schnittstelle für Unterklassen. `abstract`-Methoden müssen in jeder Unterklasse implementiert werden.',
    syntax: 'abstract class Form {\n  abstract readonly art: string;\n\n  // Abstrakte Methode - MUSS überschrieben werden\n  abstract flaeche(): number;\n  abstract umfang(): number;\n\n  // Konkrete Methode - geerbt\n  beschreibe(): string {\n    return `${this.art}: Fläche=${this.flaeche().toFixed(2)}, ` +\n           `Umfang=${this.umfang().toFixed(2)}`;\n  }\n}\n\nclass Kreis extends Form {\n  readonly art = "Kreis";\n  constructor(private radius: number) { super(); }\n\n  flaeche()  { return Math.PI * this.radius ** 2; }\n  umfang()   { return 2 * Math.PI * this.radius; }\n}\n\nclass Quadrat extends Form {\n  readonly art = "Quadrat";\n  constructor(private seite: number) { super(); }\n\n  flaeche()  { return this.seite ** 2; }\n  umfang()   { return 4 * this.seite; }\n}\n\n// const f = new Form(); // FEHLER: Abstrakte Klasse!\nconst k = new Kreis(5);\nconsole.log(k.beschreibe()); // Kreis: Fläche=78.54, Umfang=31.42',
    language: 'typescript',
    tags: ['abstract', 'klasse', 'class', 'vererbung', 'interface', 'template method', 'überschreiben'],
  },
  {
    id: 'ts-index-signatures',
    name: 'Index Signatures',
    category: 'Interfaces & Types',
    description: 'Index Signatures erlauben Objekte mit dynamischen Schlüsseln (wie ein Dictionary). Ideal wenn die genauen Property-Namen zur Compile-Zeit unbekannt sind.',
    syntax: '// Einfache Index Signature\ninterface Wörterbuch {\n  [schlüssel: string]: string;\n}\n\nconst übersetzung: Wörterbuch = {\n  hello: "Hallo",\n  world: "Welt",\n  // Beliebige weitere String-Keys möglich\n};\n\n// Mit bekannten und dynamischen Properties\ninterface Konfiguration {\n  host: string;          // Bekannte Property\n  port: number;          // Bekannte Property\n  [option: string]: string | number; // Beliebige weitere\n}\n\n// Numerischer Index\ninterface StringArray {\n  [index: number]: string;\n}\n\nconst arr: StringArray = ["Hallo", "Welt"];\nconsole.log(arr[0]); // "Hallo"\n\n// Record<K, V> ist syntaktischer Zucker\ntype Noten = Record<string, number>; // = { [k: string]: number }\nconst klasse: Noten = { Max: 2, Anna: 1, Tom: 3 };',
    language: 'typescript',
    tags: ['index signature', 'dynamisch', 'dictionary', 'record', 'schlüssel', 'key', 'beliebig'],
  },
  {
    id: 'ts-declaration-merging',
    name: 'Declaration Merging & Module Augmentation',
    category: 'Interfaces & Types',
    description: 'TypeScript erlaubt das Zusammenführen von Deklarationen gleichen Namens. Interfaces mit gleichem Namen werden automatisch gemergt. Damit können externe Typen erweitert werden.',
    syntax: '// Interface Merging: beide Definitionen werden zusammengeführt\ninterface Tier {\n  name: string;\n}\ninterface Tier {          // gleicher Name – wird gemergt!\n  alter: number;\n}\n// Ergebnis: { name: string; alter: number }\nconst t: Tier = { name: "Rex", alter: 3 }; // OK\n\n// Module Augmentation: externe Bibliothek erweitern\n// z.B. Express Request-Typ erweitern\ndeclare module "express" {\n  interface Request {\n    benutzer?: { id: number; name: string };\n  }\n}\n// Jetzt hat req.benutzer TypeScript-Support!\n\n// Global augmentation\ndeclare global {\n  interface Window {\n    meineAnwendung: { version: string };\n  }\n}\nwindow.meineAnwendung = { version: "1.0.0" }; // kein Fehler',
    language: 'typescript',
    tags: ['declaration merging', 'interface merging', 'module augmentation', 'extend', 'global', 'erweitern'],
  },
  {
    id: 'ts-namespace',
    name: 'Namespaces & Ambient Declarations',
    category: 'Moderne Syntax',
    description: 'Namespaces gruppieren zusammengehörige Typen und Funktionen unter einem Namen. `declare` beschreibt Typen für externen JavaScript-Code ohne Typ-Definitionen.',
    syntax: '// Namespace: Code gruppieren\nnamespace Mathe {\n  export const PI = 3.14159;\n\n  export function kreisFlaeche(r: number): number {\n    return PI * r * r;\n  }\n\n  // Verschachtelter Namespace\n  export namespace Statistik {\n    export function mittelwert(werte: number[]): number {\n      return werte.reduce((a, b) => a + b, 0) / werte.length;\n    }\n  }\n}\n\nconsole.log(Mathe.PI);\nconsole.log(Mathe.kreisFlaeche(5));\nconsole.log(Mathe.Statistik.mittelwert([1, 2, 3])); // 2\n\n// Ambient Declarations: externe JS-Libs typisieren\ndeclare const __VERSION__: string;\n\ndeclare function legacyFunktion(x: number): void;\n\ndeclare class AltesPlugin {\n  init(options: Record<string, unknown>): void;\n  destroy(): void;\n}',
    language: 'typescript',
    tags: ['namespace', 'ambient', 'declare', 'global', 'gruppieren', 'extern', 'd.ts'],
  },
  // ── Weitere Utility Types ───────────────────────────────────────────────────
  {
    id: 'ts-exclude-extract',
    name: 'Exclude, Extract, NonNullable',
    category: 'Utility Types',
    description: '`Exclude<T, U>` entfernt Typen aus einer Union. `Extract<T, U>` behält nur die gemeinsamen. `NonNullable<T>` entfernt `null` und `undefined`. Alle werden mit Conditional Types implementiert.',
    syntax: 'type Alle = "rot" | "grün" | "blau" | "gelb";\n\n// Exclude: bestimmte Typen entfernen\ntype OhneRot = Exclude<Alle, "rot">;       // "grün" | "blau" | "gelb"\ntype OhneRotGruen = Exclude<Alle, "rot" | "grün">; // "blau" | "gelb"\n\n// Extract: nur gemeinsame Typen behalten\ntype Primär = "rot" | "blau" | "gelb";\ntype Gemeinsam = Extract<Alle, Primär>; // "rot" | "blau" | "gelb"\n\n// NonNullable: null/undefined entfernen\ntype MitNull = string | null | undefined;\ntype NurString = NonNullable<MitNull>; // string\n\n// Praktisches Beispiel\ntype EventName = "click" | "focus" | "blur" | "mouseenter";\ntype TouchEvents = Exclude<EventName, "mouseenter">;\n// "click" | "focus" | "blur"\n\n// Implementierung (zum Lernen)\ntype MeinExclude<T, U> = T extends U ? never : T;\ntype MeinExtract<T, U> = T extends U ? T : never;',
    language: 'typescript',
    tags: ['Exclude', 'Extract', 'NonNullable', 'utility type', 'union', 'entfernen', 'filtern'],
  },
  {
    id: 'ts-return-params',
    name: 'ReturnType & Parameters',
    category: 'Utility Types',
    description: '`ReturnType<T>` extrahiert den Rückgabetyp einer Funktion. `Parameters<T>` extrahiert die Parameter als Tupel. Nützlich, um Typen aus bestehenden Funktionen abzuleiten ohne Duplizierung.',
    syntax: 'function holeDaten(url: string, timeout: number): Promise<string> {\n  return fetch(url).then(r => r.text());\n}\n\n// ReturnType: Rückgabetyp extrahieren\ntype Ergebnis = ReturnType<typeof holeDaten>; // Promise<string>\ntype Entpackt = Awaited<ReturnType<typeof holeDaten>>; // string\n\n// Parameters: Parameter-Typen als Tupel\ntype Params = Parameters<typeof holeDaten>;\n// [url: string, timeout: number]\n\ntype ErsterParam = Parameters<typeof holeDaten>[0]; // string\n\n// ConstructorParameters: Konstruktor-Parameter\nclass Benutzer {\n  constructor(public name: string, public alter: number) {}\n}\ntype BenutzerParams = ConstructorParameters<typeof Benutzer>;\n// [name: string, alter: number]\n\n// InstanceType: Instanztyp einer Klasse\ntype BenutzerInstanz = InstanceType<typeof Benutzer>;\n// Benutzer',
    language: 'typescript',
    tags: ['ReturnType', 'Parameters', 'ConstructorParameters', 'InstanceType', 'Awaited', 'utility', 'ableiten'],
  },
  {
    id: 'ts-readonly-tuple',
    name: 'Readonly Arrays & Tupel',
    category: 'Typen',
    description: 'Tupel sind Arrays mit fester Länge und bekannten Typen an bestimmten Positionen. `readonly` verhindert Mutationen. Labeled Tuples (seit TS 4.0) verbessern die Lesbarkeit.',
    syntax: '// Tupel: feste Länge und Typen\ntype RGB = [number, number, number];\nconst rot: RGB = [255, 0, 0]; // OK\n// const falsch: RGB = [255, 0]; // Fehler: zu kurz!\n\n// Labeled Tuple (lesbarer)\ntype Koordinate = [x: number, y: number, z?: number];\nconst punkt: Koordinate = [10, 20];   // OK\nconst punkt3d: Koordinate = [1, 2, 3]; // OK\n\n// readonly Tupel\nconst unveraenderlich = [1, 2, 3] as const;\n// unveraenderlich[0] = 9; // Fehler!\n\ntype ReadonlyRGB = Readonly<RGB>;\n// Alle Elemente unveränderlich\n\n// Readonly Array\nfunction berechne(zahlen: readonly number[]): number {\n  // zahlen.push(1); // Fehler! Kein Mutieren\n  return zahlen.reduce((a, b) => a + b, 0);\n}\n\n// Destrukturierung\nconst [r, g, b]: RGB = [128, 64, 255];\nconsole.log(r, g, b); // 128 64 255',
    language: 'typescript',
    tags: ['tuple', 'tupel', 'readonly', 'fixed-length', 'labeled tuple', 'as const', 'destrukturierung'],
  },
  {
    id: 'ts-discriminated-union',
    name: 'Discriminated Unions (Pattern)',
    category: 'Typen',
    description: 'Discriminated Unions verwenden ein gemeinsames "Tag"-Feld (z.B. `kind` oder `type`), um TypeScript zu helfen, den genauen Typ in einer Union zu bestimmen. Macht switch-Blöcke typsicher.',
    syntax: '// Gemeinsames "kind"-Feld → Diskriminator\ntype Form =\n  | { kind: "kreis";    radius: number }\n  | { kind: "rechteck"; breite: number; hoehe: number }\n  | { kind: "dreieck";  basis: number; hoehe: number };\n\nfunction flaeche(form: Form): number {\n  switch (form.kind) {\n    case "kreis":     return Math.PI * form.radius ** 2;\n    case "rechteck":  return form.breite * form.hoehe;\n    case "dreieck":   return (form.basis * form.hoehe) / 2;\n    // TypeScript warnt, wenn ein case fehlt (Exhaustiveness)\n  }\n}\n\n// API-Responses typsicher modellieren\ntype ApiResult<T> =\n  | { status: "ok";    data: T }\n  | { status: "fehler"; code: number; message: string }\n  | { status: "laden" };\n\nfunction render<T>(result: ApiResult<T>): void {\n  if (result.status === "ok")     console.log(result.data);\n  if (result.status === "fehler") console.log(result.message);\n  if (result.status === "laden")  console.log("...");\n}',
    language: 'typescript',
    tags: ['discriminated union', 'tagged union', 'kind', 'switch', 'exhaustive', 'pattern', 'status'],
  },
  {
    id: 'ts-never',
    name: 'never & Exhaustiveness Checking',
    category: 'Typen',
    description: '`never` ist ein Typ ohne Werte – er repräsentiert Code der nie erreicht wird. Nützlich für Exhaustiveness Checks (vollständige switch-Blöcke) und Fehler-Typen.',
    syntax: '// never: dieser Code wird nie erreicht\nfunction fehlerWerfen(msg: string): never {\n  throw new Error(msg); // gibt nie zurück!\n}\n\n// Exhaustiveness Check\ntype Farbe = "rot" | "grün" | "blau";\n\nfunction verarbeite(f: Farbe): string {\n  switch (f) {\n    case "rot":   return "Stopp";\n    case "grün":  return "Geh";\n    case "blau":  return "Info";\n    default:\n      // Wenn alle Fälle abgedeckt → f ist never\n      // Wenn man "gelb" zu Farbe hinzufügt ohne hier zu erweitern\n      // → TypeScript-Fehler!\n      const _exhaustive: never = f;\n      return fehlerWerfen(`Unbekannte Farbe: ${f}`);\n  }\n}\n\n// never in Conditional Types\ntype OhneNil<T> = T extends null | undefined ? never : T;\ntype A = OhneNil<string | null>; // string\n\n// Filter mit never\ntype NurStrings<T> = {\n  [K in keyof T]: T[K] extends string ? K : never\n}[keyof T];',
    language: 'typescript',
    tags: ['never', 'exhaustive', 'throw', 'unreachable', 'switch', 'vollständig', 'typ-narrowing'],
  },
  {
    id: 'ts-keyof-typeof',
    name: 'keyof & typeof',
    category: 'Typen',
    description: '`keyof T` erzeugt eine Union aller Keys eines Typs. `typeof` extrahiert den Typ einer Variable. Gemeinsam erlauben sie präzise Typen aus bestehendem Code abzuleiten.',
    syntax: 'interface Einstellungen {\n  theme: "light" | "dark";\n  sprache: string;\n  version: number;\n}\n\n// keyof: alle Keys als Union\ntype EinstellungsKey = keyof Einstellungen;\n// "theme" | "sprache" | "version"\n\n// Typsichere Property-Zugriffsfunction\nfunction getEinstellung<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\n\nconst config: Einstellungen = { theme: "dark", sprache: "de", version: 2 };\nconst theme = getEinstellung(config, "theme"); // Typ: "light" | "dark"\n// getEinstellung(config, "falsch"); // Fehler!\n\n// typeof: Typ aus Variable ableiten\nconst ROUTEN = {\n  home:    "/",\n  profil:  "/profil",\n  einstellungen: "/einstellungen",\n} as const;\n\ntype Route = typeof ROUTEN;         // das Objekt-Typ\ntype RoutenKey = keyof typeof ROUTEN; // "home" | "profil" | "einstellungen"\ntype RoutenWert = (typeof ROUTEN)[RoutenKey]; // "/" | "/profil" | ...',
    language: 'typescript',
    tags: ['keyof', 'typeof', 'type operator', 'union', 'keys', 'ableiten', 'lookup type'],
  },
  {
    id: 'ts-classes-access',
    name: 'Klassen: Access Modifier & private',
    category: 'Klassen & OOP',
    description: 'TypeScript erweitert Klassen mit Access Modifiern: `public` (Standard), `private`/`#` (wirklich privat), `protected` (nur Subklassen). Kurzschreibweise im Konstruktor spart Zeilen.',
    syntax: 'class BankKonto {\n  // Kurzschreibweise: Parameter = Property\n  constructor(\n    public  readonly inhaber: string,\n    private         guthaben: number = 0,\n    protected       waehrung: string = "EUR"\n  ) {}\n\n  // Getter\n  get saldo(): string {\n    return `${this.guthaben} ${this.waehrung}`;\n  }\n\n  // Private Methode\n  private protokolliere(betrag: number): void {\n    console.log(`Transaktion: ${betrag} ${this.waehrung}`);\n  }\n\n  einzahlen(betrag: number): void {\n    if (betrag <= 0) throw new Error("Betrag muss positiv sein");\n    this.guthaben += betrag;\n    this.protokolliere(betrag);\n  }\n}\n\n// Wirklich privates Feld (JS-nativ, #)\nclass Sicher {\n  #passwort: string; // unsichtbar für alle außen\n  constructor(pw: string) { this.#passwort = pw; }\n  pruefen(pw: string): boolean { return this.#passwort === pw; }\n}\n\nconst k = new BankKonto("Max", 1000);\nconsole.log(k.saldo);     // "1000 EUR"\nconsole.log(k.inhaber);   // "Max"\n// k.guthaben;            // Fehler!',
    language: 'typescript',
    tags: ['public', 'private', 'protected', 'readonly', '#', 'getter', 'setter', 'access modifier', 'klasse'],
  },
  {
    id: 'ts-interface-vs-type',
    name: 'interface vs. type',
    category: 'Interfaces & Types',
    description: 'Beide definieren Objekt-Formen, aber mit Unterschieden. Interfaces sind erweiterbar (Declaration Merging, `extends`). Types sind flexibler (Unions, Tupel, Primitives). Faustregel: `interface` für Objekte, `type` für Unions/Tupel.',
    syntax: '// Interface: erweitert mit extends & mergt automatisch\ninterface Tier {\n  name: string;\n}\ninterface Tier {         // ← wird zusammengeführt!\n  alter: number;\n}\ninterface Hund extends Tier {\n  rasse: string;\n}\n\n// Type: flexibler, kann nicht gemergt werden\ntype ID = string | number;  // Union – nur mit type!\ntype Punkt = [number, number]; // Tupel – nur mit type!\ntype Callback = () => void;    // Funktion\n\ntype Admin = Hund & { rechte: string[] }; // Intersection\n\n// Beide für Objekte verwendbar:\ninterface ObjA { x: number; }\ntype     ObjB = { x: number; }; // Äquivalent\n\n// Wann welches?\n// ✅ interface: Klassen-Verträge, erweiterbare APIs, OOP\n// ✅ type: Unions, Tupel, mapped/conditional types, Primitives\n// 🤷 Objekte: beide funktionieren, Projekt-Konvention wählen',
    language: 'typescript',
    tags: ['interface', 'type', 'unterschied', 'extends', 'union', 'merging', 'faustregel', 'vergleich'],
  },
  {
    id: 'ts-import-types',
    name: 'import type & Type-only Imports',
    category: 'Moderne Syntax',
    description: '`import type` importiert nur den Typ, keinen Laufzeit-Code. Verhindert versehentliche Laufzeit-Imports und macht Bundles kleiner. TypeScript entfernt alle Type-only Imports automatisch.',
    syntax: '// Normaler Import: Klasse UND Typ\nimport { Benutzer } from "./benutzer";\n\n// Type-only: nur für Typüberprüfung, kein Laufzeit-Code\nimport type { Benutzer } from "./benutzer";\nimport type { BenutzerID, BenutzerRolle } from "./typen";\n\n// Inline type (gemischter Import)\nimport { erstelle, type Benutzer } from "./api";\n\n// Warum?\n// 1. Klarheit: zeigt explizit was Laufzeit- und was Typ-Code ist\n// 2. Circular Imports: type-only Imports können Kreisreferenzen lösen\n// 3. Performance: bundler können Tree-Shaking besser optimieren\n\n// Verwendung (nur im TypeScript-Kontext)\nfunction zeige(user: Benutzer): void { // Typ als Parameter\n  console.log(user.name);\n}\n\n// .d.ts Dateien: reine Typ-Deklarationsdateien\n// declare module "*.svg" {\n//   const src: string;\n//   export default src;\n// }',
    language: 'typescript',
    tags: ['import type', 'type-only', 'bundle', 'tree-shaking', 'd.ts', 'deklaration', 'laufzeit'],
  },
  {
    id: 'ts-strict-mode',
    name: 'tsconfig & strict Mode',
    category: 'Moderne Syntax',
    description: '`strict: true` in der `tsconfig.json` aktiviert alle Sicherheitsprüfungen. Die wichtigsten Optionen: `strictNullChecks`, `noImplicitAny`, `strictFunctionTypes`. `paths` ermöglicht Import-Aliase.',
    syntax: '// tsconfig.json\n{\n  "compilerOptions": {\n    "target":   "ES2022",      // JavaScript-Zielversion\n    "module":   "ESNext",      // Modul-System\n    "strict":   true,          // ALLE Checks aktivieren\n\n    // Was strict: true einschließt:\n    "strictNullChecks":      true, // null/undefined explizit\n    "noImplicitAny":         true, // kein implizites any\n    "strictFunctionTypes":   true, // Funktionstypen prüfen\n    "strictBindCallApply":   true, // bind/call/apply prüfen\n    "strictPropertyInitialization": true,\n\n    // Weitere empfohlene Optionen\n    "noUnusedLocals":        true, // Fehler bei unbenutzten Vars\n    "noUnusedParameters":    true, // Fehler bei unbenutzten Params\n    "noUncheckedIndexedAccess": true, // Array-Zugriff ist T|undefined\n    "exactOptionalPropertyTypes": true,\n\n    // Import-Aliase\n    "paths": {\n      "@/*": ["./src/*"],     // @/components statt ../../components\n      "@types/*": ["./src/types/*"]\n    }\n  }\n}',
    language: 'typescript',
    tags: ['tsconfig', 'strict', 'strictNullChecks', 'noImplicitAny', 'paths', 'compilerOptions', 'konfiguration'],
  },
  {
    id: 'ts-type-assertions',
    name: 'Type Assertions & Non-null Assertion',
    category: 'Typen',
    description: '`as Type` sagt TypeScript "ich weiß es besser". `!` (Non-null Assertion) sagt "dieser Wert ist nicht null/undefined". Beide deaktivieren Checks – sparsam verwenden!',
    syntax: '// as – Type Assertion\nconst input = document.querySelector("#mein-input");\n// TypeScript weiß: Element | null\n\n// Mit Assertion: wir sagen TypeScript den genauen Typ\nconst inputElement = input as HTMLInputElement;\nconsole.log(inputElement.value); // ✓ kein Fehler\n\n// Doppelte Assertion (umgehen wenn nötig, selten!)\nconst beliebig = input as unknown as string;\n\n// Non-null Assertion !\nfunction holeTitel(): string | null { return "Hallo"; }\n\nconst titel = holeTitel()!; // ! = "ist garantiert nicht null"\nconsole.log(titel.length); // ✓\n\n// Typischer Fall: DOM-Element garantiert vorhanden\nconst btn = document.getElementById("btn")!; // Element (nicht null)\nbtn.addEventListener("click", () => console.log("Klick!"));\n\n// satisfies: prüft Typ ohne zu ändern (besser als as!)\nconst palette = {\n  rot:  [255, 0, 0],\n  blau: "#0000ff",\n} satisfies Record<string, string | number[]>;\nconsole.log(palette.rot[0]); // 255 – Typ bleibt number[]!',
    language: 'typescript',
    tags: ['as', 'type assertion', 'non-null assertion', '!', 'satisfies', 'cast', 'erzwingen'],
  },
]

const categories = [
  'Typen',
  'Interfaces & Types',
  'Generics',
  'Utility Types',
  'Moderne Syntax',
  'Funktionen',
  'Klassen & OOP',
]

export const tsConfig: LibraryConfig = {
  id: 'typescript',
  label: 'TypeScript',
  color: '#3178c6',
  bgColor: '#ebf4ff',
  textColor: '#ffffff',
  icon: '/assets/icons/typescript.svg',
  description: 'JavaScript mit statischen Typen – sicherer, produktiver, skalierbar.',
  entries,
  categories,
}
