import type { LibraryConfig, LibraryEntry } from '../types/index.js'

const entries: LibraryEntry[] = [
  // ── Grundlagen ───────────────────────────────────────────────────────────────
  {
    id: 'c-hello-world',
    name: 'Hello World / Programmstruktur',
    category: 'Grundlagen',
    description: 'Ein C-Programm beginnt immer mit `main()`. `#include <stdio.h>` bindet die Standardbibliothek für Ein-/Ausgabe ein. `return 0` signalisiert dem OS einen erfolgreichen Programmabschluss.',
    syntax: '#include <stdio.h>  // Standard-I/O-Bibliothek\n\nint main(void) {\n    printf("Hallo, Welt!\\n");\n    return 0; // 0 = Erfolgreich\n}\n\n// Kompilieren:\n// gcc -Wall -o programm main.c\n// ./programm',
    language: 'c',
    tags: ['hello world', 'main', 'include', 'stdio', 'grundstruktur', 'anfang', 'kompilieren'],
  },
  {
    id: 'c-variables',
    name: 'Variablen & Datentypen',
    category: 'Grundlagen',
    description: 'C ist stark typisiert – jede Variable braucht einen Typ. Grundtypen: `int` (Ganzzahl), `float`/`double` (Kommazahl), `char` (Zeichen), `_Bool` (Wahrheitswert).',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    int    alter   = 20;          // Ganzzahl (4 Byte)\n    double preis   = 9.99;        // Kommazahl (8 Byte)\n    float  temp    = 36.5f;       // Kommazahl (4 Byte)\n    char   buchst  = \'A\';         // Einzelnes Zeichen\n    _Bool  aktiv   = 1;           // 1=true, 0=false\n\n    // Ausgabe mit Format-Specifier\n    printf("Alter: %d\\n", alter);\n    printf("Preis: %.2f\\n", preis);\n    printf("Buchstabe: %c\\n", buchst);\n    return 0;\n}',
    language: 'c',
    tags: ['int', 'float', 'double', 'char', 'bool', 'variable', 'datentyp', 'deklaration'],
  },
  {
    id: 'c-printf-scanf',
    name: 'printf & scanf',
    category: 'Grundlagen',
    description: '`printf()` gibt formatierten Text aus. `scanf()` liest Benutzereingaben. Format-Specifier: `%d` (int), `%f` (float), `%s` (string), `%c` (char), `%lf` (double).',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    int   zahl;\n    float komma;\n    char  name[50]; // Char-Array für Strings\n\n    // Eingabe\n    printf("Gib deinen Namen ein: ");\n    scanf("%49s", name);   // max. 49 Zeichen\n\n    printf("Gib eine Zahl ein: ");\n    scanf("%d", &zahl);    // & = Adresse der Variable!\n\n    printf("Gib eine Kommazahl ein: ");\n    scanf("%f", &komma);\n\n    // Ausgabe\n    printf("Hallo, %s! Zahl: %d, Komma: %.2f\\n",\n           name, zahl, komma);\n    return 0;\n}',
    language: 'c',
    tags: ['printf', 'scanf', 'eingabe', 'ausgabe', 'format', 'specifier', 'io'],
  },
  // ── Kontrollstrukturen ──────────────────────────────────────────────────────
  {
    id: 'c-conditionals',
    name: 'if / else / switch',
    category: 'Kontrollstrukturen',
    description: 'Bedingte Ausführung mit `if`/`else if`/`else`. `switch` eignet sich für viele diskrete Fälle und ist oft übersichtlicher als viele if-else-Ketten.',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    int note = 2;\n\n    // if-else\n    if (note == 1) {\n        printf("Sehr gut!\\n");\n    } else if (note <= 3) {\n        printf("Gut bis befriedigend\\n");\n    } else {\n        printf("Nicht bestanden\\n");\n    }\n\n    // switch\n    switch (note) {\n        case 1:  printf("Sehr gut\\n");   break;\n        case 2:  printf("Gut\\n");        break;\n        case 3:  printf("Befriedigend\\n"); break;\n        default: printf("Andere Note\\n"); break;\n    }\n    return 0;\n}',
    language: 'c',
    tags: ['if', 'else', 'switch', 'case', 'bedingung', 'verzweigung', 'kontrolle'],
  },
  {
    id: 'c-loops',
    name: 'for / while / do-while',
    category: 'Kontrollstrukturen',
    description: '`for` für bekannte Iterationsanzahl. `while` prüft vor der Ausführung. `do-while` führt mindestens einmal aus. `break` bricht ab, `continue` springt zur nächsten Iteration.',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    // for-Schleife\n    for (int i = 0; i < 5; i++) {\n        printf("%d ", i); // 0 1 2 3 4\n    }\n    printf("\\n");\n\n    // while-Schleife\n    int n = 10;\n    while (n > 0) {\n        printf("%d ", n);\n        n -= 3;\n    }\n    printf("\\n");\n\n    // do-while (mindestens 1x)\n    int x = 0;\n    do {\n        printf("x = %d\\n", x);\n        x++;\n    } while (x < 3);\n\n    return 0;\n}',
    language: 'c',
    tags: ['for', 'while', 'do-while', 'schleife', 'loop', 'iteration', 'break', 'continue'],
  },
  // ── Funktionen ───────────────────────────────────────────────────────────────
  {
    id: 'c-functions',
    name: 'Funktionen',
    category: 'Funktionen',
    description: 'Funktionen werden in C vor ihrer Verwendung deklariert (Prototyp) oder definiert. Rückgabetyp `void` bedeutet kein Rückgabewert. Parameter werden standardmäßig als Kopie übergeben (Call by Value).',
    syntax: '#include <stdio.h>\n\n// Prototyp (Deklaration vor main)\nint addiere(int a, int b);\nvoid begruesse(const char* name);\n\nint main(void) {\n    int ergebnis = addiere(3, 4);\n    printf("3 + 4 = %d\\n", ergebnis); // 7\n    begruesse("Anna");\n    return 0;\n}\n\n// Implementierung\nint addiere(int a, int b) {\n    return a + b;\n}\n\nvoid begruesse(const char* name) {\n    printf("Hallo, %s!\\n", name);\n}',
    language: 'c',
    tags: ['funktion', 'function', 'prototyp', 'return', 'parameter', 'void', 'call by value'],
  },
  // ── Arrays & Strings ─────────────────────────────────────────────────────────
  {
    id: 'c-arrays',
    name: 'Arrays',
    category: 'Arrays & Strings',
    description: 'Arrays speichern mehrere Elemente des gleichen Typs. Die Größe ist fest. In C sind Array-Namen ein Zeiger auf das erste Element. Mehrdimensionale Arrays sind möglich.',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    // Array deklarieren & initialisieren\n    int noten[5] = {1, 2, 3, 4, 5};\n    int groesse   = 5;\n\n    // Zugriff per Index (ab 0!)\n    printf("Erste Note: %d\\n", noten[0]); // 1\n    noten[2] = 10; // Wert ändern\n\n    // Schleife über Array\n    double summe = 0;\n    for (int i = 0; i < groesse; i++) {\n        summe += noten[i];\n    }\n    printf("Durchschnitt: %.1f\\n", summe / groesse);\n\n    // 2D-Array\n    int matrix[2][3] = {{1,2,3}, {4,5,6}};\n    printf("matrix[1][2] = %d\\n", matrix[1][2]); // 6\n    return 0;\n}',
    language: 'c',
    tags: ['array', 'feld', 'index', 'mehrdimensional', 'matrix', 'liste', 'größe'],
  },
  {
    id: 'c-strings',
    name: 'Strings (char-Arrays)',
    category: 'Arrays & Strings',
    description: 'In C sind Strings Char-Arrays mit einem Null-Terminator `\\0` am Ende. Die `<string.h>`-Bibliothek bietet Funktionen wie `strlen`, `strcpy`, `strcat`, `strcmp`.',
    syntax: '#include <stdio.h>\n#include <string.h>  // String-Funktionen\n\nint main(void) {\n    char gruss[20] = "Hallo";\n    char name[20]  = "Welt";\n    char ziel[40];\n\n    // Länge\n    printf("Länge: %zu\\n", strlen(gruss)); // 5\n\n    // Kopieren\n    strcpy(ziel, gruss);\n\n    // Verketten\n    strcat(ziel, ", ");\n    strcat(ziel, name);\n    strcat(ziel, "!");\n    printf("%s\\n", ziel); // "Hallo, Welt!"\n\n    // Vergleichen (0 = gleich)\n    if (strcmp(gruss, "Hallo") == 0) {\n        printf("Gleich!\\n");\n    }\n    return 0;\n}',
    language: 'c',
    tags: ['string', 'char', 'array', 'strlen', 'strcpy', 'strcat', 'strcmp', 'null-terminator'],
  },
  // ── Zeiger ───────────────────────────────────────────────────────────────────
  {
    id: 'c-pointers',
    name: 'Zeiger (Pointer)',
    category: 'Zeiger & Speicher',
    description: 'Ein Zeiger speichert die Speicheradresse einer anderen Variable. `&` gibt die Adresse, `*` dereferenziert den Zeiger (greift auf den Wert zu). Zeiger sind das mächtigste (und fehleranfälligste) Konzept in C.',
    syntax: '#include <stdio.h>\n\nvoid verdopple(int* p) {\n    *p = *p * 2; // Wert an der Adresse ändern\n}\n\nint main(void) {\n    int zahl = 10;\n    int* ptr = &zahl; // ptr zeigt auf zahl\n\n    printf("Wert:    %d\\n", zahl);  // 10\n    printf("Adresse: %p\\n", (void*)ptr);\n    printf("Wert via Pointer: %d\\n", *ptr); // 10\n\n    *ptr = 42; // zahl über Pointer ändern\n    printf("Jetzt: %d\\n", zahl); // 42\n\n    verdopple(&zahl); // Adresse übergeben\n    printf("Verdoppelt: %d\\n", zahl); // 84\n    return 0;\n}',
    language: 'c',
    tags: ['pointer', 'zeiger', 'adresse', 'dereferenzieren', 'speicher', 'referenz', '&', '*'],
  },
  {
    id: 'c-malloc',
    name: 'Dynamischer Speicher (malloc/free)',
    category: 'Zeiger & Speicher',
    description: '`malloc()` reserviert Speicher auf dem Heap zur Laufzeit. `free()` gibt ihn wieder frei. Ohne `free()` entsteht ein Speicherleck. `calloc()` initialisiert zusätzlich mit 0.',
    syntax: '#include <stdio.h>\n#include <stdlib.h>  // malloc, free, calloc\n\nint main(void) {\n    int n = 5;\n\n    // Speicher für 5 ints reservieren\n    int* arr = (int*) malloc(n * sizeof(int));\n\n    if (arr == NULL) {\n        fprintf(stderr, "Speicherfehler!\\n");\n        return 1;\n    }\n\n    for (int i = 0; i < n; i++) {\n        arr[i] = (i + 1) * 10;\n    }\n\n    for (int i = 0; i < n; i++) {\n        printf("%d ", arr[i]); // 10 20 30 40 50\n    }\n    printf("\\n");\n\n    free(arr); // Speicher freigeben!\n    arr = NULL; // Dangling Pointer vermeiden\n    return 0;\n}',
    language: 'c',
    tags: ['malloc', 'free', 'calloc', 'realloc', 'heap', 'speicher', 'dynamisch', 'memory leak'],
  },
  // ── Strukturen ───────────────────────────────────────────────────────────────
  {
    id: 'c-structs',
    name: 'Strukturen (struct)',
    category: 'Strukturen & Enums',
    description: '`struct` fasst verschiedene Datentypen zu einem zusammengesetzten Typ zusammen. Mit `typedef` kann ein kürzerer Alias vergeben werden.',
    syntax: '#include <stdio.h>\n#include <string.h>\n\n// struct definieren\ntypedef struct {\n    char name[50];\n    int  alter;\n    float note;\n} Schueler;\n\nvoid zeige(const Schueler* s) {\n    printf("%s (Alter: %d, Note: %.1f)\\n",\n           s->name, s->alter, s->note);\n}\n\nint main(void) {\n    Schueler s1;\n    strcpy(s1.name, "Max");\n    s1.alter = 16;\n    s1.note  = 1.5f;\n\n    zeige(&s1);\n\n    // Array von structs\n    Schueler klasse[3] = {\n        {"Anna", 15, 1.0f},\n        {"Tom",  16, 2.3f},\n        {"Lisa", 15, 1.7f},\n    };\n    for (int i = 0; i < 3; i++) zeige(&klasse[i]);\n    return 0;\n}',
    language: 'c',
    tags: ['struct', 'struktur', 'typedef', 'compound', 'record', 'felder', 'zusammengesetzt'],
  },
  {
    id: 'c-enums',
    name: 'Enumerationen (enum)',
    category: 'Strukturen & Enums',
    description: '`enum` definiert eine Menge benannter ganzzahliger Konstanten. Macht Code lesbarer als magische Zahlen. Der erste Wert ist standardmäßig 0.',
    syntax: '#include <stdio.h>\n\ntypedef enum {\n    MONTAG = 1,\n    DIENSTAG,\n    MITTWOCH,\n    DONNERSTAG,\n    FREITAG,\n    SAMSTAG,\n    SONNTAG\n} Wochentag;\n\nconst char* tagName(Wochentag t) {\n    switch (t) {\n        case MONTAG:    return "Montag";\n        case DIENSTAG:  return "Dienstag";\n        case MITTWOCH:  return "Mittwoch";\n        default:        return "anderer Tag";\n    }\n}\n\nint main(void) {\n    Wochentag heute = MITTWOCH;\n    printf("Heute: %s (Wert: %d)\\n",\n           tagName(heute), heute); // Mittwoch (Wert: 3)\n    return 0;\n}',
    language: 'c',
    tags: ['enum', 'konstante', 'aufzählung', 'named constants', 'wochentag'],
  },
  // ── Präprozessor ─────────────────────────────────────────────────────────────
  {
    id: 'c-preprocessor',
    name: 'Präprozessor (#define, #include)',
    category: 'Präprozessor',
    description: 'Der C-Präprozessor läuft vor der Kompilierung. `#define` definiert Makros und Konstanten. `#ifdef`/`#ifndef` ermöglichen bedingte Kompilierung (z.B. Header-Guards).',
    syntax: '#include <stdio.h>\n\n// Konstante (kein Semikolon!)\n#define PI     3.14159265\n#define MAX_N  100\n\n// Funktions-Makro\n#define QUADRAT(x) ((x) * (x))\n#define MAX(a, b)  ((a) > (b) ? (a) : (b))\n\n// Bedingte Kompilierung\n#ifdef DEBUG\n    #define LOG(msg) printf("[DEBUG] %s\\n", msg)\n#else\n    #define LOG(msg) /* nichts */\n#endif\n\nint main(void) {\n    printf("PI = %.5f\\n", PI);\n    printf("3^2 = %d\\n", QUADRAT(3));   // 9\n    printf("Max(4,7) = %d\\n", MAX(4,7)); // 7\n    LOG("Programmstart");\n    return 0;\n}',
    language: 'c',
    tags: ['define', 'include', 'ifdef', 'ifndef', 'makro', 'präprozessor', 'konstante', 'header-guard'],
  },
  // ── Datei-I/O ────────────────────────────────────────────────────────────────
  {
    id: 'c-file-io',
    name: 'Datei-I/O (fopen/fclose)',
    category: 'Datei-I/O',
    description: '`fopen()` öffnet eine Datei und gibt einen `FILE*`-Zeiger zurück. Modi: `"r"` (lesen), `"w"` (schreiben, überschreibt), `"a"` (anhängen). Immer mit `fclose()` schließen!',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    // Datei schreiben\n    FILE* f = fopen("daten.txt", "w");\n    if (f == NULL) {\n        perror("Fehler beim Öffnen");\n        return 1;\n    }\n    fprintf(f, "Zeile 1\\n");\n    fprintf(f, "Zeile 2\\n");\n    fclose(f);\n\n    // Datei lesen\n    f = fopen("daten.txt", "r");\n    if (f == NULL) { perror("Fehler"); return 1; }\n\n    char zeile[100];\n    while (fgets(zeile, sizeof(zeile), f) != NULL) {\n        printf("%s", zeile);\n    }\n    fclose(f);\n    return 0;\n}',
    language: 'c',
    tags: ['fopen', 'fclose', 'fprintf', 'fgets', 'datei', 'file', 'lesen', 'schreiben', 'io'],
  },
  {
    id: 'c-math',
    name: 'Mathematische Funktionen',
    category: 'Grundlagen',
    description: 'Die Bibliothek `<math.h>` stellt Funktionen für Potenz, Wurzel, trigonometrische und logarithmische Berechnungen bereit. Beim Linken mit `-lm` verknüpfen.',
    syntax: '#include <stdio.h>\n#include <math.h>  // gcc ... -lm\n\nint main(void) {\n    double x = 9.0;\n\n    printf("sqrt(9)    = %.2f\\n", sqrt(x));    // 3.00\n    printf("pow(2,8)   = %.0f\\n", pow(2, 8));  // 256\n    printf("fabs(-5.3) = %.1f\\n", fabs(-5.3)); // 5.3\n    printf("ceil(2.1)  = %.0f\\n", ceil(2.1));  // 3\n    printf("floor(2.9) = %.0f\\n", floor(2.9)); // 2\n    printf("log(e)     = %.2f\\n", log(M_E));   // 1.00\n    printf("sin(PI/2)  = %.2f\\n", sin(M_PI/2)); // 1.00\n\n    return 0;\n}',
    language: 'c',
    tags: ['math', 'sqrt', 'pow', 'fabs', 'ceil', 'floor', 'sin', 'cos', 'log', 'mathematik'],
  },
  // ── Zeiger (erweitert) ───────────────────────────────────────────────────────
  {
    id: 'c-function-pointers',
    name: 'Funktionszeiger',
    category: 'Funktionen',
    description: 'Funktionszeiger speichern die Adresse einer Funktion und ermöglichen es, Funktionen als Parameter zu übergeben. Basis für Callbacks und das Strategy-Pattern in C.',
    syntax: '#include <stdio.h>\n#include <stdlib.h>\n\n// Funktionszeiger-Typ: void(*)(int,int)\nvoid addiere (int a, int b) { printf("%d\\n", a + b); }\nvoid subtrahiere(int a, int b) { printf("%d\\n", a - b); }\n\n// Typedef macht den Typ lesbarer\ntypedef void (*Rechenop)(int, int);\n\nvoid berechne(int a, int b, Rechenop op) {\n    op(a, b); // Funktion über Zeiger aufrufen\n}\n\nint main(void) {\n    Rechenop op = addiere;     // Zeiger zeigt auf addiere\n    op(3, 4);                  // Ausgabe: 7\n    op = subtrahiere;          // Zeiger umleiten\n    op(10, 3);                 // Ausgabe: 7\n\n    berechne(5, 2, addiere);   // Callback\n    berechne(5, 2, subtrahiere);\n\n    // Array von Funktionszeigern (Dispatch-Tabelle)\n    Rechenop ops[] = { addiere, subtrahiere };\n    ops[0](1, 2); // 3\n    return 0;\n}',
    language: 'c',
    tags: ['funktionszeiger', 'function pointer', 'callback', 'typedef', 'dispatch', 'zeiger'],
  },
  {
    id: 'c-recursion',
    name: 'Rekursion',
    category: 'Funktionen',
    description: 'Eine Funktion, die sich selbst aufruft. Jede Rekursion braucht einen Basisfall (Abbruchbedingung). Für große Tiefen Stack-Overflow-Gefahr – dann besser Iteration nutzen.',
    syntax: '#include <stdio.h>\n\n// Fakultät: n! = n * (n-1)!\nlong long fakultaet(int n) {\n    if (n <= 1) return 1;       // Basisfall\n    return n * fakultaet(n - 1); // Rekursionsfall\n}\n\n// Fibonacci\nlong long fibonacci(int n) {\n    if (n <= 1) return n;       // 0 → 0, 1 → 1\n    return fibonacci(n-1) + fibonacci(n-2);\n}\n\n// Binärsuche (rekursiv)\nint binaersuche(int* arr, int links, int rechts, int ziel) {\n    if (links > rechts) return -1; // nicht gefunden\n    int mitte = (links + rechts) / 2;\n    if (arr[mitte] == ziel) return mitte;\n    if (arr[mitte] < ziel)\n        return binaersuche(arr, mitte+1, rechts, ziel);\n    return binaersuche(arr, links, mitte-1, ziel);\n}\n\nint main(void) {\n    printf("5! = %lld\\n", fakultaet(5));   // 120\n    printf("fib(8) = %lld\\n", fibonacci(8)); // 21\n    return 0;\n}',
    language: 'c',
    tags: ['rekursion', 'recursion', 'basisfall', 'fakultät', 'fibonacci', 'binärsuche', 'stack'],
  },
  {
    id: 'c-typedef-unions',
    name: 'Union & Bitfelder',
    category: 'Strukturen & Enums',
    description: 'Eine `union` verwendet für alle Felder denselben Speicherbereich – spart Speicher. Bitfelder in Structs packen mehrere Werte in ein einzelnes Byte – ideal für eingebettete Systeme.',
    syntax: '#include <stdio.h>\n\n// Union: alle Felder teilen denselben Speicher\nunion Wert {\n    int   ganzzahl;\n    float kommazahl;\n    char  zeichen;\n};\n\nint main(void) {\n    union Wert v;\n    v.ganzzahl = 65;\n    printf("int: %d\\n", v.ganzzahl);\n    printf("char: %c\\n", v.zeichen); // \'A\' (ASCII 65)\n    // Nur ein Feld ist gleichzeitig gültig!\n\n    // Bitfelder: mehrere Flags in einem Byte\n    struct Flags {\n        unsigned aktiv    : 1; // 1 Bit\n        unsigned sichtbar : 1;\n        unsigned lesbar   : 1;\n        unsigned schreibb : 1;\n        unsigned         : 4; // 4 Bits ungenutzt (Padding)\n    };\n\n    struct Flags f = { .aktiv = 1, .sichtbar = 1, .lesbar = 0, .schreibb = 0 };\n    printf("aktiv=%u sichtbar=%u lesbar=%u\\n",\n           f.aktiv, f.sichtbar, f.lesbar);\n    printf("Größe: %zu Byte\\n", sizeof(f)); // 1 Byte!\n    return 0;\n}',
    language: 'c',
    tags: ['union', 'bitfield', 'bitfeld', 'speicher', 'flags', 'embedded', 'struct', 'packing'],
  },
  {
    id: 'c-command-args',
    name: 'Kommandozeilenargumente',
    category: 'Grundlagen',
    description: '`argc` gibt die Anzahl der Argumente an, `argv` ist ein Array von Strings. `argv[0]` ist der Programmname, `argv[1]` das erste echte Argument.',
    syntax: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint main(int argc, char* argv[]) {\n    // argc = Anzahl Argumente (inkl. Programmname)\n    printf("Programm: %s\\n", argv[0]);\n    printf("Argumente: %d\\n", argc - 1);\n\n    for (int i = 1; i < argc; i++) {\n        printf("argv[%d] = %s\\n", i, argv[i]);\n    }\n\n    // Praktisches Beispiel: Zahl als Argument\n    if (argc < 2) {\n        fprintf(stderr, "Nutzung: %s <zahl>\\n", argv[0]);\n        return 1;\n    }\n\n    int zahl = atoi(argv[1]); // String → int\n    printf("Quadrat von %d: %d\\n", zahl, zahl * zahl);\n\n    return 0;\n}\n// Aufruf: ./programm 7\n// Ausgabe: Quadrat von 7: 49',
    language: 'c',
    tags: ['argc', 'argv', 'kommandozeile', 'argument', 'main', 'atoi', 'terminal'],
  },
  {
    id: 'c-qsort',
    name: 'Sortieren mit qsort',
    category: 'Arrays & Strings',
    description: '`qsort()` aus `<stdlib.h>` sortiert Arrays beliebiger Typen. Erwartet einen Vergleichsfunktions-Zeiger. Der Comparator gibt negativ/0/positiv zurück (wie strcmp).',
    syntax: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n// Comparator für int (aufsteigend)\nint cmpInt(const void* a, const void* b) {\n    return (*(int*)a - *(int*)b);\n}\n\n// Comparator für int (absteigend)\nint cmpIntDesc(const void* a, const void* b) {\n    return (*(int*)b - *(int*)a);\n}\n\n// Comparator für Strings\nint cmpStr(const void* a, const void* b) {\n    return strcmp(*(const char**)a, *(const char**)b);\n}\n\nint main(void) {\n    int zahlen[] = {5, 2, 8, 1, 9, 3};\n    int n = sizeof(zahlen) / sizeof(zahlen[0]);\n\n    qsort(zahlen, n, sizeof(int), cmpInt);\n    for (int i = 0; i < n; i++)\n        printf("%d ", zahlen[i]); // 1 2 3 5 8 9\n    printf("\\n");\n\n    const char* woerter[] = {"Banane", "Apfel", "Kirsche"};\n    qsort(woerter, 3, sizeof(char*), cmpStr);\n    for (int i = 0; i < 3; i++)\n        printf("%s ", woerter[i]); // Apfel Banane Kirsche\n    return 0;\n}',
    language: 'c',
    tags: ['qsort', 'sortieren', 'sort', 'comparator', 'stdlib', 'algorithmus', 'pointer'],
  },
  {
    id: 'c-sprintf',
    name: 'sprintf & sscanf',
    category: 'Arrays & Strings',
    description: '`sprintf()` schreibt formatierten Text in einen String (statt stdout). `sscanf()` liest Werte aus einem String. Ideal für String-Formatierung und einfaches Parsen.',
    syntax: '#include <stdio.h>\n#include <string.h>\n\nint main(void) {\n    char puffer[100];\n    int  alter  = 25;\n    float preis = 9.99f;\n\n    // sprintf: in String schreiben\n    sprintf(puffer, "Alter: %d, Preis: %.2f EUR", alter, preis);\n    printf("%s\\n", puffer);\n    // "Alter: 25, Preis: 9.99 EUR"\n\n    // snprintf: mit Größenbegrenzung (sicherer!)\n    char sicher[20];\n    snprintf(sicher, sizeof(sicher), "Zahl: %d", 12345);\n    printf("%s\\n", sicher); // "Zahl: 12345"\n\n    // sscanf: aus String lesen\n    const char* eingabe = "2024-12-25";\n    int jahr, monat, tag;\n    sscanf(eingabe, "%d-%d-%d", &jahr, &monat, &tag);\n    printf("Jahr: %d, Monat: %d, Tag: %d\\n",\n           jahr, monat, tag); // 2024 12 25\n\n    // String-Konvertierungen\n    char buf[50];\n    sprintf(buf, "%d", 42);       // int → String\n    int zahl = atoi("123");       // String → int\n    double d = atof("3.14");      // String → double\n    return 0;\n}',
    language: 'c',
    tags: ['sprintf', 'snprintf', 'sscanf', 'formatierung', 'string', 'parsen', 'atoi', 'atof'],
  },
  {
    id: 'c-bitwise',
    name: 'Bitweise Operatoren',
    category: 'Grundlagen',
    description: 'Bitweise Operatoren arbeiten direkt auf einzelnen Bits. Wichtig für Flags, Masken und systemnahe Programmierung. `&` AND, `|` OR, `^` XOR, `~` NOT, `<<` links-, `>>` rechtsshift.',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    unsigned int a = 0b00001100; // 12\n    unsigned int b = 0b00001010; // 10\n\n    printf("a & b  = %u\\n", a & b);  // AND:  8  (0b00001000)\n    printf("a | b  = %u\\n", a | b);  // OR:  14  (0b00001110)\n    printf("a ^ b  = %u\\n", a ^ b);  // XOR:  6  (0b00000110)\n    printf("~a     = %u\\n", ~a);     // NOT: alle Bits umkehren\n    printf("a << 1 = %u\\n", a << 1); // Links:  24 (×2)\n    printf("a >> 1 = %u\\n", a >> 1); // Rechts:  6 (÷2)\n\n    // Flags mit Bitmaskierung\n    #define LESEN    (1 << 0) // 0b001 = 1\n    #define SCHREIBEN (1 << 1) // 0b010 = 2\n    #define AUSFUEHREN (1 << 2) // 0b100 = 4\n\n    int rechte = LESEN | SCHREIBEN; // 0b011 = 3\n    if (rechte & LESEN)     printf("Darf lesen\\n");\n    if (rechte & AUSFUEHREN) printf("Darf ausführen\\n");\n\n    rechte |= AUSFUEHREN;  // Flag setzen\n    rechte &= ~SCHREIBEN;  // Flag löschen\n    rechte ^= LESEN;       // Flag umschalten\n    return 0;\n}',
    language: 'c',
    tags: ['bitweise', 'bitwise', 'AND', 'OR', 'XOR', 'NOT', 'shift', 'flags', 'maske', 'bitmask'],
  },
  {
    id: 'c-realloc',
    name: 'realloc & Speicher-Management',
    category: 'Zeiger & Speicher',
    description: '`realloc()` ändert die Größe eines bereits allozierten Speicherblocks. Falls nicht genug Platz vorhanden, wird an eine neue Stelle kopiert. Immer Rückgabewert prüfen!',
    syntax: '#include <stdio.h>\n#include <stdlib.h>\n\nint main(void) {\n    int kapazitaet = 4;\n    int anzahl     = 0;\n    int* arr = (int*) malloc(kapazitaet * sizeof(int));\n    if (!arr) { perror("malloc"); return 1; }\n\n    // Elemente hinzufügen, bei Bedarf vergrößern\n    for (int i = 1; i <= 8; i++) {\n        if (anzahl == kapazitaet) {\n            kapazitaet *= 2; // Kapazität verdoppeln\n            int* tmp = (int*) realloc(arr, kapazitaet * sizeof(int));\n            if (!tmp) {\n                free(arr);  // alten Speicher freigeben!\n                return 1;\n            }\n            arr = tmp;\n            printf("Größe verdoppelt auf %d\\n", kapazitaet);\n        }\n        arr[anzahl++] = i * 10;\n    }\n\n    for (int i = 0; i < anzahl; i++)\n        printf("%d ", arr[i]); // 10 20 30 ... 80\n    printf("\\n");\n\n    free(arr);\n    arr = NULL; // Dangling Pointer verhindern\n    return 0;\n}',
    language: 'c',
    tags: ['realloc', 'malloc', 'free', 'dynamisch', 'speicher', 'heap', 'resize', 'memory management'],
  },
  {
    id: 'c-header-files',
    name: 'Header-Dateien & Mehrere Quelldateien',
    category: 'Präprozessor',
    description: 'Größere C-Projekte teilen Code auf mehrere Dateien auf. Header-Dateien (`.h`) deklarieren Funktionen, Quelldateien (`.c`) implementieren sie. Include-Guards verhindern Doppeleinbindung.',
    syntax: '// ── mathe.h ──────────────────────────────\n#ifndef MATHE_H   // Include-Guard\n#define MATHE_H\n\n// Nur Deklarationen (Prototypen)\nint addiere(int a, int b);\nint multipliziere(int a, int b);\ndouble kreisFlaeche(double r);\n\n// Konstante\n#define MATHE_PI 3.14159265\n\n#endif // MATHE_H\n\n// ── mathe.c ──────────────────────────────\n#include "mathe.h"\n\nint addiere(int a, int b) { return a + b; }\nint multipliziere(int a, int b) { return a * b; }\ndouble kreisFlaeche(double r) {\n    return MATHE_PI * r * r;\n}\n\n// ── main.c ───────────────────────────────\n#include <stdio.h>\n#include "mathe.h"  // eigene Header: Anführungszeichen\n\nint main(void) {\n    printf("%d\\n", addiere(3, 4));      // 7\n    printf("%.2f\\n", kreisFlaeche(5)); // 78.54\n    return 0;\n}\n// Kompilieren: gcc main.c mathe.c -o programm',
    language: 'c',
    tags: ['header', '.h', 'include guard', 'ifndef', 'define', 'mehrere dateien', 'modular', 'prototyp'],
  },
  {
    id: 'c-type-casting',
    name: 'Typumwandlung (Casting)',
    category: 'Grundlagen',
    description: 'In C gibt es implizite (automatische) und explizite Typumwandlungen. Explizites Casting mit `(Typ)` ist wichtig bei Division von Ganzzahlen und Zeiger-Umwandlungen.',
    syntax: '#include <stdio.h>\n\nint main(void) {\n    // Implizite Konvertierung (passiert automatisch)\n    int i = 42;\n    double d = i;   // int → double, kein Datenverlust\n    printf("%.1f\\n", d); // 42.0\n\n    // Explicit Cast (wichtig!)\n    int a = 7, b = 2;\n\n    // ❌ Ganzzahldivision (Ergebnis: 3, nicht 3.5)\n    double falsch = a / b;\n    printf("%.1f\\n", falsch); // 3.0 !\n\n    // ✅ Cast vor der Division\n    double richtig = (double)a / b;\n    printf("%.1f\\n", richtig); // 3.5 ✓\n\n    // Zeiger-Cast (void* → int*)\n    void* ptr = &a;\n    int*  iptr = (int*)ptr;\n    printf("%d\\n", *iptr); // 7\n\n    // Truncation: Nachkommastellen gehen verloren!\n    double pi = 3.14159;\n    int abgeschnitten = (int)pi;\n    printf("%d\\n", abgeschnitten); // 3 (kein Runden!)\n    return 0;\n}',
    language: 'c',
    tags: ['casting', 'typumwandlung', 'cast', 'implizit', 'explizit', 'division', 'truncation', 'konvertierung'],
  },
  {
    id: 'c-random',
    name: 'Zufallszahlen (rand/srand)',
    category: 'Grundlagen',
    description: '`rand()` generiert Pseudozufallszahlen. `srand(time(NULL))` setzt den Seed, damit sich die Zahlen bei jedem Programmstart unterscheiden.',
    syntax: '#include <stdio.h>\n#include <stdlib.h>\n#include <time.h>\n\nint main(void) {\n    // Seed setzen (einmalig!)\n    srand((unsigned)time(NULL));\n\n    // Zufallszahl 0 bis RAND_MAX\n    printf("rand() = %d\\n", rand());\n\n    // Zufallszahl in einem Bereich [min, max]\n    // Formel: rand() % (max - min + 1) + min\n    int min = 1, max = 6;\n    for (int i = 0; i < 5; i++) {\n        int wuerfel = rand() % (max - min + 1) + min;\n        printf("Würfel: %d\\n", wuerfel);\n    }\n\n    // Zufällige Float-Zahl [0.0, 1.0)\n    double zufall = (double)rand() / ((double)RAND_MAX + 1.0);\n    printf("Float: %.4f\\n", zufall);\n\n    // Zufälliges Element aus Array\n    const char* optionen[] = {"Schere", "Stein", "Papier"};\n    int idx = rand() % 3;\n    printf("Wahl: %s\\n", optionen[idx]);\n    return 0;\n}',
    language: 'c',
    tags: ['rand', 'srand', 'random', 'zufall', 'zufallszahl', 'time', 'seed', 'würfel'],
  },
  {
    id: 'c-const-volatile',
    name: 'const, volatile & restrict',
    category: 'Zeiger & Speicher',
    description: '`const` schützt vor versehentlichem Ändern. `volatile` verhindert Compiler-Optimierungen bei Hardware-Registern. `restrict` sagt dem Compiler, dass kein Aliasing stattfindet (Optimierungshinweis).',
    syntax: '#include <stdio.h>\n\n// const: Wert nicht veränderbar\nconst int MAX = 100;\n// MAX = 200; // Fehler!\n\n// const-Zeiger (Kombinationen)\nint x = 5, y = 10;\n\nconst int* p1 = &x;   // Wert ist const, Zeiger nicht\n// *p1 = 7;           // Fehler! Wert nicht änderbar\np1 = &y;              // OK: Zeiger umlenken\n\nint* const p2 = &x;   // Zeiger ist const, Wert nicht\n*p2 = 7;              // OK: Wert ändern\n// p2 = &y;           // Fehler! Zeiger festgelegt\n\nconst int* const p3 = &x; // Beides const\n\n// volatile: Compiler-Optimierung verhindern\n// Wichtig bei Hardware-Registern (embedded):\nvolatile int status_register; // immer aus Speicher lesen!\n\n// restrict: kein Aliasing (nur in Funktionen)\nvoid kopiere(int* restrict ziel, const int* restrict quelle, int n) {\n    for (int i = 0; i < n; i++)\n        ziel[i] = quelle[i]; // Compiler kann optimieren\n}',
    language: 'c',
    tags: ['const', 'volatile', 'restrict', 'qualifizierer', 'optimierung', 'zeiger', 'hardware', 'embedded'],
  },
  {
    id: 'c-linked-list',
    name: 'Verkettete Liste (Linked List)',
    category: 'Zeiger & Speicher',
    description: 'Verkettete Listen sind dynamische Datenstrukturen, bei denen jeder Knoten einen Wert und einen Zeiger auf den nächsten Knoten enthält. Flexibler als Arrays bei häufigem Einfügen/Löschen.',
    syntax: '#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Knoten {\n    int           wert;\n    struct Knoten* naechster;\n} Knoten;\n\n// Knoten vorne einfügen\nKnoten* einfuegen(Knoten* kopf, int wert) {\n    Knoten* neu = (Knoten*) malloc(sizeof(Knoten));\n    neu->wert      = wert;\n    neu->naechster = kopf;\n    return neu; // neuer Kopf\n}\n\nvoid ausgeben(const Knoten* k) {\n    while (k != NULL) {\n        printf("%d -> ", k->wert);\n        k = k->naechster;\n    }\n    printf("NULL\\n");\n}\n\nvoid freigeben(Knoten* k) {\n    while (k != NULL) {\n        Knoten* tmp = k->naechster;\n        free(k);\n        k = tmp;\n    }\n}\n\nint main(void) {\n    Knoten* liste = NULL;\n    liste = einfuegen(liste, 30);\n    liste = einfuegen(liste, 20);\n    liste = einfuegen(liste, 10);\n    ausgeben(liste); // 10 -> 20 -> 30 -> NULL\n    freigeben(liste);\n    return 0;\n}',
    language: 'c',
    tags: ['linked list', 'verkettete liste', 'knoten', 'node', 'zeiger', 'dynamisch', 'datenstruktur'],
  },
  {
    id: 'c-scope-storage',
    name: 'Scope & Speicherklassen',
    category: 'Grundlagen',
    description: '`auto` (Standard), `static` (behält Wert zwischen Aufrufen, oder beschränkt auf Datei), `extern` (externe Variable), `register` (Hinweis an Compiler). Lokale Variablen liegen auf dem Stack, `static`/globale im Data-Segment.',
    syntax: '#include <stdio.h>\n\n// Global: in allen Funktionen sichtbar\nint globaleVar = 0;\n\n// static auf Dateiebene: nur in dieser .c-Datei sichtbar\nstatic int dateiIntern = 42;\n\nvoid zaehler(void) {\n    // static local: behält Wert zwischen Aufrufen!\n    static int count = 0; // Initialisierung nur einmal!\n    count++;\n    printf("Aufrufe: %d\\n", count);\n}\n\nvoid block_scope(void) {\n    int x = 1;           // Block-Scope\n    {\n        int x = 2;       // Andere Variable (shadowing)\n        printf("Innen: %d\\n", x); // 2\n    }\n    printf("Außen: %d\\n", x); // 1\n}\n\nint main(void) {\n    zaehler(); // Aufrufe: 1\n    zaehler(); // Aufrufe: 2\n    zaehler(); // Aufrufe: 3\n    block_scope();\n    return 0;\n}',
    language: 'c',
    tags: ['static', 'extern', 'auto', 'scope', 'speicherklasse', 'lokal', 'global', 'stack', 'lebensdauer'],
  },
  {
    id: 'c-string-advanced',
    name: 'Erweiterte String-Operationen',
    category: 'Arrays & Strings',
    description: 'Wichtige weitere String-Funktionen aus `<string.h>` und `<ctype.h>`: `strtok` (Tokenizer), `strstr` (Suchen), `memcpy`/`memmove`, Zeichenklassen-Tests.',
    syntax: '#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n\nint main(void) {\n    // strtok: String aufteilen (Tokenizer)\n    char satz[] = "Hallo,Welt,wie,geht,es";\n    char* token = strtok(satz, ","); // Erstes Token\n    while (token != NULL) {\n        printf("%s\\n", token);      // Hallo, Welt, ...\n        token = strtok(NULL, ",");  // Weiter\n    }\n\n    // strstr: Teilstring suchen\n    const char* text = "Ich lerne C-Programmierung";\n    char* pos = strstr(text, "C-");\n    if (pos) printf("Gefunden bei Index: %ld\\n", pos - text);\n\n    // memcpy / memmove\n    char src[10] = "Hallo";\n    char dst[10];\n    memcpy(dst, src, strlen(src) + 1); // Null-Terminator mitkopieren!\n    printf("%s\\n", dst); // Hallo\n\n    // <ctype.h>: Zeichenklassen\n    char c = \'A\';\n    printf("isupper: %d\\n", isupper(c)); // 1\n    printf("tolower: %c\\n", tolower(c)); // \'a\'\n    printf("isdigit(\'5\'): %d\\n", isdigit(\'5\')); // 1\n    return 0;\n}',
    language: 'c',
    tags: ['strtok', 'strstr', 'memcpy', 'memmove', 'ctype', 'isupper', 'isdigit', 'tolower', 'tokenizer'],
  },
]

const categories = [
  'Grundlagen',
  'Kontrollstrukturen',
  'Funktionen',
  'Arrays & Strings',
  'Zeiger & Speicher',
  'Strukturen & Enums',
  'Präprozessor',
  'Datei-I/O',
]

export const cConfig: LibraryConfig = {
  id: 'c',
  label: 'C',
  color: '#6b8da8',
  bgColor: '#f0f4f8',
  textColor: '#ffffff',
  icon: '/assets/icons/c.svg',
  description: 'Die Mutter moderner Programmiersprachen – effizient, systemnahe, mächtig.',
  entries,
  categories,
}
